import initialState from '../../initialState';

function EmployerDashboardReducer(state = initialState.employerDashboard, action) {
  switch (action.type) {
    default: {
      return state;
    }

  }
}
export default EmployerDashboardReducer;
